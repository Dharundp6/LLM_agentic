---
phase: 02-court-corpus-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebook_kaggle.ipynb
autonomous: true
requirements:
  - COURT-01
  - COURT-02
  - COURT-03
  - COURT-05
  - FUSE-05
tags: [court-corpus, bm25s, german-filter, ram-profiling, retrieval]

must_haves:
  truths:
    - "Court corpus is loaded from court_considerations.csv and filtered to German-only rows before indexing"
    - "German filter logs count and percentage (D-08)"
    - "bm25s court index is built in 500K-doc chunks with RSS logged after each chunk (D-09)"
    - "Tokenized list is deleted after index build with gc.collect() to free ~11 GB"
    - "BM25 court retrieves top-200 candidates per query with per-query timing logged (D-10)"
    - "Per-signal val F1 is logged for laws-dense, laws-BM25, and court-BM25 independently (D-12 partial)"
    - "USE_COURT_CORPUS feature flag gates all court cells"
  artifacts:
    - path: "notebook_kaggle.ipynb"
      provides: "Cell 9 (court load + filter + index) and Cell 10 (court BM25 retrieval + per-signal F1)"
      contains: "bm25_court"
  key_links:
    - from: "notebook_kaggle.ipynb Cell 9 (new)"
      to: "court_considerations.csv via DATA_DIR"
      via: "pd.read_csv"
      pattern: "court_considerations\\.csv"
    - from: "notebook_kaggle.ipynb Cell 9 (new)"
      to: "tokenize_for_bm25_de from Cell 3"
      via: "function call on court text"
      pattern: "tokenize_for_bm25_de"
    - from: "notebook_kaggle.ipynb Cell 10 (new)"
      to: "bm25_query_texts from Cell 4"
      via: "dict lookup per query (D-13)"
      pattern: "bm25_query_texts\\[qid\\]"
---

<objective>
Add court corpus BM25 retrieval to the notebook pipeline. Insert two new cells
(Cell 9: court load + German filter + bm25s index build with RAM profiling;
Cell 10: per-query BM25 court retrieval top-200 + per-signal val F1 logging)
between existing Cell 8 (bm25s laws) and existing Cell 9 (RRF fuse). This shifts
all downstream cells by +2. Also update Cell 0 to set USE_COURT_CORPUS=True and
add BM25_COURT_K=200 hyperparameter.

Purpose: Court decisions represent 40.6% of val gold citations. Without court
retrieval, the pipeline has zero recall on those citations. This is the single
highest-impact addition in the entire roadmap.

Output: Cells 9-10 of notebook_kaggle.ipynb with court BM25 pipeline; existing
cells 9-13 renumbered to 11-15; `bm25_court_ids` dict and `court_de_citations`
list available for downstream consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-court-corpus-coverage/02-CONTEXT.md
@.planning/phases/02-court-corpus-coverage/02-RESEARCH.md
@notebook_kaggle.ipynb
@solution.py

<interfaces>
<!-- Key variables and functions available from Phase 1 cells that this plan depends on. -->

From Cell 0 (feature flags and hyperparameters):
```python
USE_COURT_CORPUS = False  # FLIP TO True in this plan
BM25_LAWS_K = 100
RRF_K_CONST = 60
RERANK_K = 150
```

From Cell 1 (device and helpers):
```python
DEVICE = torch.device("cuda")
def unload(*objs): ...  # Free GPU memory
```

From Cell 3 (tokenization helpers):
```python
def canonicalize(s): ...          # Swiss citation normalizer
def extract_legal_codes(text): ... # Legal code extraction from queries
def tokenize_for_bm25_de(text): ... # German-aware BM25 tokenizer with CharSplit
GERMAN_STOP = frozenset(...)       # German stopwords set
```

From Cell 4 (cached translations):
```python
translations = {}       # qid -> german_translation
bm25_query_texts = {}   # qid -> german_translation + extracted_codes
_target_ids = [...]      # list of all val+test query IDs
q_en_canon = {}          # qid -> canonicalized English query
```

From Cell 7 (dense laws results):
```python
dense_laws_ids = {}  # qid -> list[int] of laws row indices
```

From Cell 8 (BM25 laws results):
```python
bm25_laws_ids = {}   # qid -> list[int] of laws row indices
bm25_laws = bm25s.BM25()  # laws BM25 index object
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert Cell 9 -- Court corpus load, German filter, bm25s index build with RAM profiling</name>
  <files>notebook_kaggle.ipynb</files>
  <read_first>
    - notebook_kaggle.ipynb (full current state -- 14 cells, understand Cell 8 output and Cell 9 input)
    - .planning/phases/02-court-corpus-coverage/02-RESEARCH.md (German filter heuristic, RAM budget, chunked build pattern, bm25s API)
    - .planning/phases/02-court-corpus-coverage/02-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
Use nbformat to insert a NEW cell at position 9 (between current Cell 8 and current Cell 9).
All cells currently at index 9+ shift to index 10+. This task ONLY inserts Cell 9.

**Cell 0 update first:** Before inserting the new cell, update Cell 0 to:
1. Change `USE_COURT_CORPUS = False` to `USE_COURT_CORPUS = True`
2. Add `BM25_COURT_K = 200` in the hyperparameters section (after `RERANK_K = 150`)

**New Cell 9 source -- Court corpus load + German filter + bm25s index build:**

The cell must:

1. **Define `is_german()` heuristic** (per D-08 and RESEARCH.md validated heuristic):
   - Use a frozen set of ~50 common German stopwords
   - Check first 20 words of text; return True if >= 2 are German stopwords
   - Reject texts shorter than 30 chars as non-German (stubs)

2. **Define `log_rss()` helper** for D-09 RAM profiling:
   - On Linux (Kaggle): use `resource.getrusage(resource.RUSAGE_SELF).ru_maxrss / 1e6` (KB to GB)
   - On Windows (local): use `psutil.Process().memory_info().rss / 1e9` with try/except fallback to "N/A"
   - Print with label and warn if > 25 GB

3. **Load court CSV** inside `if USE_COURT_CORPUS:` gate:
   - `court = pd.read_csv(DATA_DIR / "court_considerations.csv")`
   - `fillna("")` on both columns
   - Log row count and time
   - Call `log_rss("after court CSV load")`

4. **German filter** (D-08):
   - `de_mask = court["text"].apply(is_german)`
   - `court_de = court[de_mask].reset_index(drop=True)`
   - Print: `f"Court corpus German filter: {len(court_de):,}/{len(court):,} rows ({len(court_de)/len(court)*100:.1f}%)"`
   - `del court; gc.collect()` to free full DataFrame
   - Call `log_rss("after German filter + del full court")`

5. **Tokenize in 500K-row chunks** (D-09):
   - `CHUNK_SIZE = 500_000`
   - For each chunk: canonicalize `citation + " " + text`, then `tokenize_for_bm25_de()`
   - Extend master `court_bm25_tokens` list
   - After each chunk: print progress count and call `log_rss(f"chunk {n}")`

6. **Build bm25s index:**
   - `bm25_court = bm25s.BM25(); bm25_court.index(court_bm25_tokens)`
   - Log build time
   - **CRITICAL:** `del court_bm25_tokens; gc.collect()` immediately after index()
   - Call `log_rss("after bm25s.index() + del tokens")`

7. **Extract court_de_citations list** before deleting DataFrame:
   - `court_de_citations = court_de["citation"].tolist()`
   - Then `del court_de; gc.collect()` to free the filtered DataFrame (~2 GB)
   - Call `log_rss("post-cleanup steady state")`

8. **else branch** (when USE_COURT_CORPUS=False):
   - `bm25_court = None; court_de_citations = []; print("USE_COURT_CORPUS=False; skipping court index.")`

IMPORTANT: Use `nb.cells.insert(9, nbformat.v4.new_code_cell(source))` to insert,
NOT replace. This preserves all existing cells and shifts them down.

Do NOT use iterrows() for the tokenization loop -- it is extremely slow on 1.6M rows.
Use list comprehension on `court_de["citation"].values` and `court_de["text"].values`
arrays for the chunked tokenization. Pattern:
```python
citations = court_de["citation"].values
texts = court_de["text"].values
for chunk_start in range(0, len(court_de), CHUNK_SIZE):
    chunk_cits = citations[chunk_start:chunk_start + CHUNK_SIZE]
    chunk_txts = texts[chunk_start:chunk_start + CHUNK_SIZE]
    chunk_tokens = [
        tokenize_for_bm25_de(canonicalize(f"{c} {t}"))
        for c, t in zip(chunk_cits, chunk_txts)
    ]
    court_bm25_tokens.extend(chunk_tokens)
```
  </action>
  <verify>
    <automated>python -c "import nbformat; nb=nbformat.read('notebook_kaggle.ipynb',4); assert len(nb.cells)>=15, f'expected 15+ cells, got {len(nb.cells)}'; c0=nb.cells[0].source; assert 'USE_COURT_CORPUS = True' in c0 or 'USE_COURT_CORPUS=True' in c0, 'USE_COURT_CORPUS not flipped'; assert 'BM25_COURT_K' in c0, 'BM25_COURT_K missing from Cell 0'; c9=nb.cells[9].source; assert 'def is_german' in c9, 'is_german missing'; assert 'def log_rss' in c9, 'log_rss missing'; assert 'court_considerations.csv' in c9, 'court CSV load missing'; assert 'bm25_court.index' in c9 or 'bm25_court .index' in c9, 'bm25s index build missing'; assert 'del court_bm25_tokens' in c9, 'token cleanup missing'; assert 'court_de_citations' in c9, 'citation list extraction missing'; assert 'USE_COURT_CORPUS' in c9, 'feature flag gate missing'; print('OK')"</automated>
  </verify>
  <done>Cell 9 inserted with court corpus load, German filter with logged count/percentage, chunked tokenization with RSS profiling, bm25s index build, token cleanup via del+gc.collect(), and court_de_citations extraction. Cell 0 updated with USE_COURT_CORPUS=True and BM25_COURT_K=200. Notebook has 15+ cells.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert Cell 10 -- Per-query BM25 court retrieval + per-signal val F1 logging</name>
  <files>notebook_kaggle.ipynb</files>
  <read_first>
    - notebook_kaggle.ipynb (after Task 1 -- verify Cell 9 exists with court index)
    - .planning/phases/02-court-corpus-coverage/02-RESEARCH.md (Cell 8c pattern, per-signal F1 logging, retrieval timing D-10)
    - .planning/phases/02-court-corpus-coverage/02-CONTEXT.md (D-10, D-12)
  </read_first>
  <action>
Use nbformat to insert a NEW cell at position 10 (after the Cell 9 just created in Task 1).
All cells currently at index 10+ shift to index 11+. After this task the notebook has 16 cells.

**New Cell 10 source -- BM25 court retrieval + per-signal F1:**

The cell must:

1. **Per-query BM25 court retrieval** inside `if USE_COURT_CORPUS and bm25_court is not None:` gate:
   - Print stage header: `f"\n=== Stage 3c: BM25 court retrieval (top-{BM25_COURT_K}) ==="`
   - Loop over `_target_ids`:
     - Get `q_text = bm25_query_texts[qid]` (D-13: reuse cached translations)
     - Tokenize: `q_tokens = tokenize_for_bm25_de(q_text)`
     - Guard empty tokens: `if not q_tokens: bm25_court_ids[qid] = []; continue`
     - Time each query: `qt0 = time.time()`
     - Retrieve: `results, scores = bm25_court.retrieve([q_tokens], k=BM25_COURT_K)`
     - Store: `bm25_court_ids[qid] = [int(x) for x in results[0]]`
     - Accumulate query time
   - Log total time and mean per-query time in ms
   - Warn if mean exceeds 2.0 seconds (D-10)
   - Print sample: `f"  sample qid={_target_ids[0]}  top-5 court idx={bm25_court_ids[_target_ids[0]][:5]}"`

2. **Per-signal val F1 logging** (D-12, FUSE-05):
   - Define `signal_val_f1(signal_ids_dict, id_to_citation_fn, val_df, k_max=60)`:
     - For each val query: get gold set from `gold_citations` column (split by ";")
     - Map signal IDs to citation strings via `id_to_citation_fn(idx)`
     - Build val_results list, call `calibrate_top_k()` (from Cell 11, but define
       a local copy here to avoid forward reference -- OR read Cell 11 and verify
       calibrate_top_k is defined there; if it is, this cell must come AFTER Cell 11
       which is wrong. Instead, define a lightweight inline version):
     ```python
     def _quick_f1(val_results, k_max=60):
         best_f1, best_k = 0.0, 1
         golds = [r["gold"] for r in val_results]
         for k in range(1, k_max + 1):
             preds = [set(r["ranked"][:k]) for r in val_results]
             f1 = macro_f1(golds, preds)
             if f1 > best_f1:
                 best_f1, best_k = f1, k
         return best_k, best_f1
     ```
     Wait -- `macro_f1` is defined in Cell 13 (was Cell 11, now shifted +2). That is
     AFTER this cell. So we need a local `macro_f1` too. Define both `macro_f1` and
     `_quick_f1` locally in this cell (they are small, ~15 lines total). This avoids
     forward references.

   - Compute per-signal F1 for val queries only (not test):
     - `laws_id_to_cit = lambda idx: canonicalize(laws.iloc[idx]["citation"])`
     - `court_id_to_cit = lambda idx: canonicalize(court_de_citations[idx])`
     - For each signal (dense_laws_ids, bm25_laws_ids, bm25_court_ids), build val_results and call `_quick_f1`
   - Print D-12 comparison table:
     ```
     === Per-Signal Val F1 ===
     laws-dense:  F1=X.XXXX @ top-K
     laws-BM25:   F1=X.XXXX @ top-K
     court-BM25:  F1=X.XXXX @ top-K
     ```
     (The `fused-3sig` line is printed in Plan 02 after RRF fusion.)

3. **else branch** (when USE_COURT_CORPUS=False):
   - `bm25_court_ids = {qid: [] for qid in _target_ids}`
   - `print("USE_COURT_CORPUS=False; skipping court retrieval.")`

IMPORTANT: The local `macro_f1` defined here is identical to the one in Cell 13
(was Cell 11). This duplication is intentional to avoid forward references. Add a
comment: `# local copy -- canonical version in Cell 13 (Stage 6a)`.
  </action>
  <verify>
    <automated>python -c "import nbformat; nb=nbformat.read('notebook_kaggle.ipynb',4); assert len(nb.cells)>=16, f'expected 16+ cells, got {len(nb.cells)}'; c10=nb.cells[10].source; assert 'bm25_court_ids' in c10, 'bm25_court_ids missing'; assert 'bm25_court.retrieve' in c10, 'court retrieval missing'; assert 'Per-Signal Val F1' in c10, 'D-12 table missing'; assert 'macro_f1' in c10, 'macro_f1 missing'; assert 'BM25_COURT_K' in c10, 'BM25_COURT_K missing'; assert 'USE_COURT_CORPUS' in c10, 'feature flag gate missing'; print('OK')"</automated>
  </verify>
  <done>Cell 10 inserted with per-query BM25 court retrieval (top-200), per-query timing with 2s warning threshold, and D-12 per-signal val F1 comparison table for all three signals. bm25_court_ids dict available for downstream RRF fusion. Notebook has 16 cells.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> notebook | court_considerations.csv read from DATA_DIR; treated as trusted Kaggle competition data |
| RAM budget | 2.47M-row corpus tokenization peaks at ~17.6 GB; must stay under 30 GB Kaggle limit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Denial of Service | RAM OOM during court index build | mitigate | Chunked tokenization with RSS logging per D-09; del court_bm25_tokens + gc.collect() after index(); warn at 25 GB threshold; stub filter fallback if needed |
| T-02-01-02 | Tampering | court_considerations.csv data integrity | accept | Kaggle-provided competition data; fillna("") handles nulls; German filter removes non-matching content |
| T-02-01-03 | Denial of Service | Slow BM25 retrieval on 1.6M corpus | mitigate | Per-query timing logged with 2s warning threshold (D-10); bm25s vectorized retrieval expected <100ms/query |
| T-02-01-04 | Information Disclosure | none | accept | No secrets; offline Kaggle notebook |
</threat_model>

<verification>
1. `python -c "import nbformat; nb=nbformat.read('notebook_kaggle.ipynb',4); print(len(nb.cells))"` prints 16
2. Cell 0 contains `USE_COURT_CORPUS = True` and `BM25_COURT_K = 200`
3. Cell 9 contains `is_german`, `log_rss`, court CSV load, German filter, chunked tokenization, bm25s index build, token cleanup
4. Cell 10 contains `bm25_court_ids`, `bm25_court.retrieve`, per-signal val F1 table
5. Cell 9 is gated by `USE_COURT_CORPUS`; Cell 10 is gated by `USE_COURT_CORPUS and bm25_court is not None`
6. Notebook JSON is valid: `nbformat.read('notebook_kaggle.ipynb', 4)` succeeds
</verification>

<success_criteria>
- Court corpus loaded, filtered to German-only (~65-67%), logged with count and percentage
- bm25s court index built in 500K chunks with RSS profiled after each chunk
- Tokenized list deleted after index build (del + gc.collect)
- BM25 court retrieves top-200 per query with timing logged and 2s warning
- Per-signal val F1 logged for laws-dense, laws-BM25, court-BM25
- All code gated by USE_COURT_CORPUS feature flag
- Notebook has 16 valid cells
</success_criteria>

<output>
After completion, create `.planning/phases/02-court-corpus-coverage/02-01-SUMMARY.md`
</output>
